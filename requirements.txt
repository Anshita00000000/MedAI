# MedAI — Steps 0–4 Dependencies
# Install with: pip install -r requirements.txt

# ── Step 0: Data Acquisition ────────────────────────────────────────────────

# HuggingFace dataset downloading & streaming
datasets>=2.19.0

# Required by datasets for Parquet / Arrow files
pyarrow>=14.0.0

# HTTP requests used internally by datasets
requests>=2.31.0

# Progress bars shown during HuggingFace downloads
tqdm>=4.66.0

# Used by datasets for hub authentication (optional but avoids warnings)
huggingface-hub>=0.23.0

# ── Step 3: Entity Extraction (scispaCy — optional) ─────────────────────────
# If scispaCy is not installed, EntityExtractor automatically falls back to
# the built-in rule-based engine (stdlib only, no extra dependencies).
#
# To enable scispaCy NER, run:
#   pip install scispacy
#   pip install https://s3-us-west-2.amazonaws.com/ai2-s3-scispacy/releases/v0.5.3/en_core_sci_md-0.5.3.tar.gz

# scispacy>=0.5.3
# https://s3-us-west-2.amazonaws.com/ai2-s3-scispacy/releases/v0.5.3/en_core_sci_md-0.5.3.tar.gz

# ── Step 4: SOAP Note Generation (Google Gemini — optional) ─────────────────
# If google-generativeai is not installed, SOAPGenerator automatically falls
# back to the built-in template engine (stdlib only, no extra dependencies).
#
# To enable Gemini SOAP generation, run:
#   pip install google-genai>=1.0.0
# and set the GOOGLE_API_KEY environment variable.

google-genai>=1.0.0

# ── Step 6: Reporting Engine (WeasyPrint — optional) ────────────────────────
# If weasyprint is not installed, PDFGenerator automatically falls back to
# saving HTML files (stdlib only, no extra dependencies).
#
# To enable PDF generation, run:
#   pip install weasyprint>=60.0
# Note: WeasyPrint also requires system libraries (Pango, Cairo, etc.)

weasyprint>=60.0

# ── Step 7: REST API ─────────────────────────────────────────────────────────

# FastAPI web framework + ASGI server
fastapi>=0.110.0
uvicorn>=0.29.0

# Required by FastAPI for multipart/form-data (file uploads)
python-multipart>=0.0.9

# ── Testing ──────────────────────────────────────────────────────────────────

pytest>=8.0.0
httpx>=0.27.0
